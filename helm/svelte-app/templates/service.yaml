apiVersion: v1
kind: Service
metadata:
  name: {{ include "svelte-app.fullname" . }}
  labels:
    {{- include "svelte-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "svelte-app.selectorLabels" . | nindent 4 }}
