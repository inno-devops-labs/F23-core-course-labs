apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "svelte-app.fullname" . }}-test-connection"
  labels:
    {{- include "svelte-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "svelte-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
