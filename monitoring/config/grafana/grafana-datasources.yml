apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    version: 1
    editable: false

  - name: Prometheus
    label: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus-host:9090
    isDefault: true