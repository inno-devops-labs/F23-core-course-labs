---
- name: Update apt cache.
  become: true
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install pip.
  become: true
  ansible.builtin.apt:
    name: python3-pip
    state: present
