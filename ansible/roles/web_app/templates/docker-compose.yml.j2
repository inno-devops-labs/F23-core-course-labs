version: "3.6"
services:
  app:
    image: {{ image }}
{% if ports is defined %}
    ports:
{% for port in ports %}
      - "{{ port }}"
{% endfor %}
{% endif %}
    volumes:
      - data:/counter.csv
