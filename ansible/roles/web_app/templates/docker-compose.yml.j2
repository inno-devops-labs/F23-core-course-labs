services:
  web_app:
    hostname: web_app
    image: {{ web_app_image }}
    restart: always
    ports:
      - "80:{{ web_app_port }}"
    command: "{{ web_app_command }}"
    volumes:
      - app_python_data:/app/data

volumes:
  app_python_data:
