version: "3.8"
services:
  {{ service_name }}:
    image: {{ app_container_image }}
    container_name: {{ app_container_name }}
    ports: {{ app_container_ports }}
    volumes:
      - {{ app_visits_file }}:/data