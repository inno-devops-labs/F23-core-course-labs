services:
  web_app:
    hostname: web_app
    image: ign19ht/dev-ops-labs:latest
    restart: always
    ports:
      - "8000:8000"
    command: "python3 main.py"
    volumes:
      - app_python_data:/app/data

volumes:
  app_python_data:
