version: '3.7'
services:
    {{ service_name }}:
    image: {{ image_name }}
    ports:
      - "{{ host_port }}:{{ internal_port }}"

    volumes:
        - 'volume_{{ service_name }}:/app/volume/'

volumes:
    'volume_{{ service_name }}:'
