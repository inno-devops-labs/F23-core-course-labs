version: '3.9'
services:
    {{ app_name }}:
        image: {{ docker_image }}
        container_name: {{ app_name }}
        restart: unless-stopped
        ports:
            - "{{ host_port }}:8000"
        volumes:
            - {{ project_root }}/visits:/tmp/visits
