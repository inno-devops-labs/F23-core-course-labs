version: '3'
services:
  web_app:
    container_name: {{ container_name }}
    image: {{ image }}
    ports:
        - {{ ports }}
    environment:
        - TZ={{ timezone }}
    volumes:
      - data:/data

volumes:
  data:
