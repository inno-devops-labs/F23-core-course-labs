version: "3"
services:
  app:
    image: {{ docker_image }}
{% if ports is defined %}
    ports:
{% for port in ports %}
      - "{{ port }}"
{% endfor %}
{% endif %}
     volumes:
       - {{ service_name }}_data:/myproject/resources

 volumes:
   {{ service_name }}_data:
