services:
  web_{{ app_name | default(app) }}:
    image: "{{ docker_image }}"
    container_name: "{{ docker_container_name }}"
    ports:
      - "{{ external_port }}:{{ internal_port }}"
    restart: unless-stopped
    volumes:
      - './visits_dir/:/{{ app_name }}/visits_dir/:rw'
