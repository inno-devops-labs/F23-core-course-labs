apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-golang-chart.fullname" . }}
  labels:
    {{- include "app-golang-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-golang-chart.selectorLabels" . | nindent 4 }}
