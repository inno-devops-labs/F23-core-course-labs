apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-golang-chart.fullname" . }}-test-connection"
  labels:
    {{- include "app-golang-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-golang-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
