apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-csharp.fullname" . }}
  labels:
    {{- include "app-csharp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-csharp.selectorLabels" . | nindent 4 }}
