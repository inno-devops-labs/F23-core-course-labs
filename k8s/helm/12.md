# Lab 12

```
PS G:\git\core-course-labs> kubectl get po
NAME                          READY   STATUS    RESTARTS       AGE
app-csharp-10lfkm4ko2-mt23o   1/1     Running   0              15s
app-python-45adsa23w5-hg6ds   1/1     Running   0              78s
```

```
PS G:\git\core-course-labs> kubectl exec app-python-45adsa23w5-hg6ds   -- cat /config.json
{
    "test":"test",
}
```

```
PS G:\git\core-course-labs> kubectl describe po app-python-45adsa23w5-hg6ds
    ..........
    Environment:
      CUSTOM_SECRET:     SECRET-ONE
      SECRET:            <set to the key 'token' in secret 'real-secret'>           Optional: false
      SPECIAL_TYPE_KEY:  <set to the key 'test' of config map 'config-py'>  Optional: false
    Mounts:
      /config.json from config-py (rw,path="config.json")
      /data from counter-py (rw)
      /var/run/secrets/kubernetes.io/serviceaccount from kube-api-access-gbjzf (ro)
....................................
Volumes:
  config-py:
    Type:      ConfigMap (a volume populated by a ConfigMap)
    Name:      config-py
    Optional:  false
  counter-py:
    Type:       EmptyDir (a temporary directory that shares a pod's lifetime)
    Medium:
    SizeLimit:  500Mi
  ......
```

```
dmpru@dmpru:/mnt/g/git/core-course-labs/k8s/helm/app-python$ kubectl exec app-python-45adsa23w5-hg6ds -- env | grep SPECIAL
SPECIAL_TYPE_KEY=test
```

# Bonus

```
PS G:\git\core-course-labs> kubectl exec app-csharp-10lfkm4ko2-mt23o -- cat /config.json
{
    "test":"test",
}
```

```
dmpru@dmpru:/mnt/g/git/core-course-labs/k8s/helm/app-python$ kubectl exec app-csharp-10lfkm4ko2-mt23o -- env | grep SPECIAL
SPECIAL_TYPE_KEY=test
```
