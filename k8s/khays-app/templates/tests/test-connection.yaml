apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "khays-app.fullname" . }}-test-connection"
  labels:
    {{- include "khays-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "khays-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
