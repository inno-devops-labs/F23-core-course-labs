apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-app-helm.fullname" . }}
  labels:
    {{- include "rust-app-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rust-app-helm.selectorLabels" . | nindent 4 }}
