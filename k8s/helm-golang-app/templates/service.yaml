apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-golang-app.fullname" . }}
  labels:
    {{- include "helm-golang-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-golang-app.selectorLabels" . | nindent 4 }}
