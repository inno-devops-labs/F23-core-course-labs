apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-elixir.fullname" . }}
  labels:
    {{- include "app-elixir.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-elixir.selectorLabels" . | nindent 4 }}
