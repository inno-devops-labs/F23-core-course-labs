apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-library.fullname" . }}
  labels:
    {{- include "app-library.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-library.selectorLabels" . | nindent 4 }}
