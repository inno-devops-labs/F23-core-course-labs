apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-elixir.fullname" . }}-test-connection"
  labels:
    {{- include "app-elixir.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-elixir.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
