apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-library.fullname" . }}-test-connection"
  labels:
    {{- include "app-library.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-library.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
