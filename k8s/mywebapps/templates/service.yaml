apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysebapps.fullname" . }}
  labels:
    {{- include "mysebapps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysebapps.selectorLabels" . | nindent 4 }}