apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cpp-app.fullname" . }}-test-connection"
  labels:
    {{- include "cpp-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cpp-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
